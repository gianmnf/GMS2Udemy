/// @description Insert description here

event_inherited();

//Independente do que for feito aqui você
//ainda herda os elementos do seu pai

//Meus Comandos
if(x != clamp(x, xstart-30, xstart+30)){ //Clamp checa valores para uma variavel
	hspeed *= -1;
}